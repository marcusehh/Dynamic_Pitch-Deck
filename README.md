Main Py is the most up to date file at any given time.
This program is a claude/antigravity enabled version built from my Auto-DCF repo.

Just paste " -m pip install -r requirements.txt" in terminal.
